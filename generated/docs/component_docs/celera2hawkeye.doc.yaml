---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: celera2hawkeye
extracted_at: "2026-02-19T15:39:24"
source_files:
  - celera2hawkeye.config
  - celera2hawkeye.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: file / conversion
  parameters:
    OUTPUT_PREFIX: ""
    # Any other command line options 
    OTHER_OPTS: ""
  input:
    # Format of input to the toAmos component
    # Valid formats include:
    # 
    # 	1.) -a: asm
    # 	2.) -s: sequence (FASTA format)
    # 	3.) -q: qual 
    # 	4.) -ace: ACE format
    # 	5.) -f: celera-assembler frg file
    # 
    # Multiple input formats may be specified by adding a space between
    # each format i.e. -s human.fasta -q human.qual	
    INPUT_FILES: ""
  output:
    OUTPUT_TOKEN: default
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
  component:
    COMPONENT_NAME: celera2hawkeye
    DESCRIPTION: none
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    # The version,revision,tag here is set by an interpolated CVS tag
    VERSION: $Name$
    RELEASE_TAG: $Name$
    REVISION: "$Revision: 5801 $"
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    ITERATOR1: i1
    ITERATOR1_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.$;ITERATOR1$;.xml
    # Distributed options
    GROUP_COUNT: 150
    NODISTRIB: 0
    # Skip these specific steps in the workflow
    SKIP_WF_COMMAND: ""
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - Preprocessing
  command_sets:
    - celera2hawkeye workflow
  steps:
    - name: create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: create temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;TMP_DIR$;
    - name: toAmos
      type: RunUnixCommand
      executable: $;TOAMOS_EXEC$;
      arg: $;INPUT_FILES$; -o $;OUTPUT_DIRECTORY$;/$;OUTPUT_PREFIX$;.afg $;OTHER_OPTS$;
      params:
        - key: stdout
          value: $;TMP_DIR$;/to_amos.stdout
        - key: stderr
          value: $;TMP_DIR$;/to_amos.stderr
    - name: bank-transact
      type: RunUnixCommand
      executable: $;BANK_TRANSACT_EXEC$;
      arg: -m $;OUTPUT_DIRECTORY$;/$;OUTPUT_PREFIX$;.afg -b $;OUTPUT_DIRECTORY$;/$;OUTPUT_PREFIX$;.bnk -c
      params:
        - key: stdout
          value: $;TMP_DIR$;/celera_to_hawkeye.bank_transact.stdout
        - key: stderr
          value: $;TMP_DIR$;/celera_to_hawkeye.bank_transact.stderr
    - name: cleanup afg file
      type: RunUnixCommand
      executable: rm
      arg: $;OUTPUT_DIRECTORY$;/$;OUTPUT_PREFIX$;.afg
