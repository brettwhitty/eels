---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: scripture
extracted_at: "2026-02-19T15:34:47"
source_files:
  - scripture.config
  - scripture.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: rnaseq
  parameters:
    ALIGNMENT: ""
    PAIRED_END_FILE: ""
    # a file with the cromosome names and lengths
    CHRM_SIZE_FILE: ""
    # a directory with .fa fasta files containing sequences of the chromosomes, like <chromosome>.fa
    CHRM_SEQUENCE_DIR: ""
    MIN_SPLICE_SUPPORT_READS: 1
    OTHER_SCRIPTURE_OPTIONS: ""
  input:
    # the following is only used when iterating over an INPUT_DIRECTORY
    INPUT_FILE: ""
  output:
    OUTPUT_TOKEN: default
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
  component:
    COMPONENT_NAME: scripture
    DESCRIPTION: none
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    ITERATOR1: i1
    ITERATOR1_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.$;ITERATOR1$;.xml
    # Distributed options
    GROUP_COUNT: 150
    NODISTRIB: 0
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - Preprocessing
    - Processing
    - "<type>RunUnixCommand</type>"
  command_sets:
    - scripture
  steps:
    - name: Create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: Create temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;TMP_DIR$;
    - name: Scripture wrapper script
      type: RunUnixCommand
      executable: $;BIN_DIR$;/scripture.sh
      arg: $;SCRIPTURE_EXEC$; $;OUTPUT_DIRECTORY$; $;ALIGNMENT$; $;PAIRED_END_FILE$; $;CHRM_SIZE_FILE$; $;CHRM_SEQUENCE_DIR$; $;MIN_SPLICE_SUPPORT_READS$; $;OTHER_SCRIPTURE_OPTIONS$;
      params:
        - key: stderr
          value: $;OUTPUT_DIRECTORY$;/scripture_stderr
        - key: stdout
          value: $;OUTPUT_DIRECTORY$;/scripture_stdout
