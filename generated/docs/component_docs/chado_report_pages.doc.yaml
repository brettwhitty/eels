---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: chado_report_pages
extracted_at: "2026-02-19T15:39:24"
source_files:
  - chado_report_pages.config
  - chado_report_pages.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: database / extraction
  parameters:
    USERNAME: chado_admin
    PASSWORD: chado_admin99
    # Name of chado database
    DATABASE: $;PROJECT$;
    # Relational database management system type e.g. sybase or postgresql
    DATABASE_TYPE: sybase
    # Server name on which the database resides
    SERVER: SYBPROD
    # User can specify a new-line separated list of model feat_name values
    MODEL_LIST_FILE: ""
  output:
    OUTPUT_TOKEN: default
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    REPORTFILE: $;OUTPUT_DIRECTORY$;/report.txt
  component:
    COMPONENT_NAME: chado_report_pages
    DESCRIPTION: none
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    # The version,revision,tag here is set by an interpolated CVS tag
    VERSION: 2.0
    RELEASE_TAG: $Name$
    REVISION: "$Revision: 5717 $"
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    ITERATOR1: i1
    ITERATOR1_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.$;ITERATOR1$;.xml
    # Distributed options
    GROUP_COUNT: 5
    NODISTRIB: 0
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - Preprocessing
    - Iterator1 for chado2bsml
  command_sets:
    - chado_report_pages workflow
  steps:
    - name: create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: create temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;TMP_DIR$;
    - name: Generate chado report pages for database '$;PROJECT$;' on server '$;SERVER$;'
      type: RunUnixCommand
      executable: $;BIN_DIR$;/generateChadoReportPages
      params:
        - key: --username
          value: $;USERNAME$;
        - key: --password
          value: $;PASSWORD$;
        - key: --database
          value: $;DATABASE$;
        - key: --server
          value: $;SERVER$;
        - key: --model_list_file
          value: $;MODEL_LIST_FILE$;
        - key: --reportfile
          value: $;REPORTFILE$;
        - key: --logfile
          value: $;TMP_DIR$;/generateChadoReportPages.pl.log
        - key: stdout
          value: $;TMP_DIR$;/generateChadoReportPages.pl.stdout
        - key: stderr
          value: $;TMP_DIR$;/generateChadoReportPages.pl.stderr
