---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: cummerbund
extracted_at: "2026-02-19T15:39:24"
source_files:
  - cummerbund.config
  - cummerbund.xml
  - cummerbund.i1.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: RNA-Seq
  parameters:
    # path to R statistical package binary directory [/usr/local/packages/R-2.15.2/bin]
    R_BIN_DIR: $;R_BIN$;
    # additonal R parameters
    R_PARAMS: ""
    # path to R Script for CummeRbund Analysis. Optional [cummerbund_isoform_analysis.R]
    R_SCRIPT: $;R_SCRIPT_PATH$;
    # use --v for verbose summary
    OTHER_ARGS: ""
  input:
    # path to list file of cuffdiff files
    # format : filelist of "*isoform_exp.diff"
    INPUT_FILE: ""
    # path to list of tab-delimited sample information files
    INPUT_FILE_LIST: ""
    # path to directory of multiple tab-delimited sample information files
    INPUT_DIRECTORY: ""
    # the following is only used when iterating over an INPUT_DIRECTORY
    INPUT_EXTENSION: info
  output:
    # use a unique sample name
    OUTPUT_TOKEN: default
    # output directory for component results
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    # list of PDF figures for each instance
    OUTPUT_PDF_FIGURES: $;OUTPUT_DIRECTORY$;/$;COMPONENT_NAME$;.pdf.list
    # list of PNG figures for each instance
    OUTPUT_PNG_FIGURES: $;OUTPUT_DIRECTORY$;/$;COMPONENT_NAME$;.png.list
  component:
    COMPONENT_NAME: cummerbund
    DESCRIPTION: generates visualization PDF and png image for given cuffdiff analysis results
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    # CummeRbund version
    VERSION: CummbeRbund2.11
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    ITERATOR1: i1
    ITERATOR1_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.$;ITERATOR1$;.xml
    # Distributed options
    GROUP_COUNT: 150
    NODISTRIB: 0
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - Processing
    - Iterator
    - Postprocessing
  command_sets:
    - cummerbund
  includes:
    - "file=\"$;DOCS_DIR$;/file_iterator_template.xml\" keys=\"$;ITERATOR_NAME$;=ITERATOR1,$;ITERATOR_XML$;=ITERATOR1_XML\""
  steps:
    - name: create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: create temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;TMP_DIR$;
    - name: create PDF figures list
      type: RunUnixCommand
      executable: $;BIN_DIR$;/generate_list_file
      arg: --v
      params:
        - key: --directory
          value: $;OUTPUT_DIRECTORY$;
        - key: --regex
          value: "\".*\\.pdf\""
        - key: --output_list
          value: $;OUTPUT_PDF_FIGURES$;
        - key: stdout
          value: $;TMP_DIR$;/generate_list_file.pdf.stdout
        - key: stderr
          value: $;TMP_DIR$;/generate_list_file.pdf.stderr
    - name: create PNG figure list
      type: RunUnixCommand
      executable: $;BIN_DIR$;/generate_list_file
      arg: --skip_error --v
      params:
        - key: --directory
          value: $;OUTPUT_DIRECTORY$;
        - key: --regex
          value: "\".*\\.png\""
        - key: --output_list
          value: $;OUTPUT_PNG_FIGURES$;
        - key: stdout
          value: $;TMP_DIR$;/generate_list_file.png.stdout
        - key: stderr
          value: $;TMP_DIR$;/generate_list_file.png.stderr

# Iterator i1 - per-input processing steps
iterator_i1:
  command_sets:
    - cummerbund
  steps:
    - name: cummerbund
      type: RunUnixCommand
      executable: $;BIN_DIR$;/cummerbund
      arg: $;OTHER_ARGS$;
      params:
        - key: --infile
          value: $;INPUT_FILE$;
        - key: --listfile
          value: "'$;INPUT_FILE_LIST$;'"
        - key: --outdir
          value: $;OUTPUT_DIRECTORY$;/$;ITERATOR_NAME$;/g$;GROUP_NUMBER$;
        - key: --r_bin_dir
          value: "'$;R_BIN_DIR$;'"
        - key: --r_params
          value: "'$;R_PARAMS$;'"
        - key: --r_script
          value: $;R_SCRIPT$;
        - key: stdout
          value: $;OUTPUT_DIRECTORY$;/$;ITERATOR_NAME$;/g$;GROUP_NUMBER$;/cummerbund.stdout
        - key: stderr
          value: $;OUTPUT_DIRECTORY$;/$;ITERATOR_NAME$;/g$;GROUP_NUMBER$;/cummerbund.stderr

# Perl script documentation (POD)
perl_scripts:
  - script: generate_list_file.pl
    pod: |
  =head1 NAME
  
  generate_list_file.pl - script to generate a list of files in a directory that
                          satisfy the given regular expression.
  
  =head1 SYNOPSIS
  
      generate_list_file.pl --d search_directory --r regex 
                            [--o output_listfile] [--gz] [--v]
  
      parameters in [] are optional
      do NOT type the carets when specifying options
  
  =head1 OPTIONS
  
      --d <search directory>         = /path/to/directory to begin the search at
  
      --r <regex>                    = regular expression criterion to filter files
  
      --o <output_listfile>          = /path/to/output list of files found. Optional. [stdout]
  
      --se                           = do not die on error. Optional
  
      --gz                           = include gzipped files in the search output. Optional
  
      --v                            = generate runtime messages. Optional
  
  =head1 DESCRIPTION
  
  The script uses the Unix find command to search for specific files within a directory tree.
  
  =head1 AUTHOR
  
   Amol Carl Shetty
   Bioinformatics Software Engineer II
   Institute of Genome Sciences
   University of Maryland
   Baltimore, Maryland 21201
  - script: cummerbund.pl
    pod: |
  =head1 NAME
  
      cummerbund.pl  -       Uses R cummerbund package to generate plots from cuffdiff 
                             analysis.
  
  =head1 SYNOPSIS
  
      cummerbund.pl          --i <input_list> [--rs R script]  
                             [--o outdir] [--v] [--rb R_bin_dir]
                             [--rp R parameters]
  
      parameters in [] are optional
      do NOT type the carets when specifying options
  
  =head1 OPTIONS
      
      --i  <input>           = List of cuffdiff output file.
  
      --o <output dir>       = /path/to/output directory. Optional.[PWD]
  
      --rs [R script]        = /path/to/Rscript. Optional.
  
      --rb <R_bin_dir>       = /path/to/R binary. Optional. [/usr/local/bin]
  
      --rp <R_parameters>    = additonal R parameters. Optional. [--vanilla]
  
      --v                    = generate runtime messages. Optional
  
  =head1 DESCRIPTION
  
  Uses R cummerbund package to generate plots from cuffdiff analysis.
  
  =head1 AUTHOR
  
   Priti Kumari
   Bioinformatics Software Engineer 
   Institute for Genome Sciences
   University of Maryland
   Baltimore, Maryland 21201
