---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: samtools_reference_index
extracted_at: "2026-02-19T15:34:47"
source_files:
  - samtools_reference_index.config
  - samtools_reference_index.xml
  - samtools_reference_index.i1.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: RNA-Seq
  parameters:
    # path to samtools package binary directory
    SAMTOOLS_BIN_DIR: $;SAMTOOLS_BIN$;
    # use --v for verbose summary
    OTHER_ARGS: --v
  input:
    # path to reference FastA file
    INPUT_FILE: ""
    # path to list of reference FastA files
    INPUT_FILE_LIST: ""
    # path to directory of multiple reference FastA files
    INPUT_DIRECTORY: ""
    # the following is only used when iterating over an INPUT_DIRECTORY
    INPUT_EXTENSION: fa
  output:
    # use a unique sample name
    OUTPUT_TOKEN: default
    # output directory for component results
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
  component:
    COMPONENT_NAME: samtools_reference_index
    DESCRIPTION: generates samtools index for the given reference FastA file
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    # samtools version
    VERSION: samtools version 0.1.18
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    ITERATOR1: i1
    ITERATOR1_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.$;ITERATOR1$;.xml
    # Distributed options
    GROUP_COUNT: 150
    NODISTRIB: 0
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - Processing
    - Iterator
  command_sets:
    - samtools_reference_index
  includes:
    - "file=\"$;DOCS_DIR$;/file_iterator_template.xml\" keys=\"$;ITERATOR_NAME$;=ITERATOR1,$;ITERATOR_XML$;=ITERATOR1_XML\""
  steps:
    - name: create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: create temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;TMP_DIR$;

# Iterator i1 - per-input processing steps
iterator_i1:
  command_sets:
    - samtools_reference_index
  steps:
    - name: samtools_reference_index
      type: RunUnixCommand
      executable: $;BIN_DIR$;/samtools_reference_index
      arg: $;OTHER_ARGS$;
      params:
        - key: --reffile
          value: $;I_FILE_PATH$;
        - key: --outdir
          value: $;OUTPUT_DIRECTORY$;/$;ITERATOR_NAME$;/g$;GROUP_NUMBER$;
        - key: --samtools_bin_dir
          value: $;SAMTOOLS_BIN_DIR$;
        - key: stdout
          value: $;OUTPUT_DIRECTORY$;/$;ITERATOR_NAME$;/g$;GROUP_NUMBER$;/samtools_reference_index.stdout
        - key: stderr
          value: $;OUTPUT_DIRECTORY$;/$;ITERATOR_NAME$;/g$;GROUP_NUMBER$;/samtools_reference_index.stderr

# Perl script documentation (POD)
perl_scripts:
  - script: samtools_reference_index.pl
    pod: |
  =head1 NAME
  
  samtools_reference_index.pl - script to generate samtools reference for reference FastA file
  
  =head1 SYNOPSIS
  
      samtools_reference_index.pl --r reference_file [--o outdir] [--sb samtools_bin_dir] [--v]
  
      parameters in [] are optional
      do NOT type the carets when specifying options
  
  =head1 OPTIONS
  
      --r <reference_file>           = /path/to/reference fasta file to index.
  
      --o <output dir>               = /path/to/output directory. Optional. [present working directory]
  
      --sb <samtools_bin_dir>        = /path/to/samtools_build binary. Optional. [/usr/local/bin]
  
      --v                            = generate runtime messages. Optional
  
  =head1 DESCRIPTION
  
  The script executes the samtools faidx script from the samtools software package.
  
  =head1 AUTHOR
  
   Amol Carl Shetty
   Bioinformatics Software Engineer II
   Institute of Genome Sciences
   University of Maryland
   Baltimore, Maryland 21201
