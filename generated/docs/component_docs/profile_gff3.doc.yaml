---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: profile_gff3
extracted_at: "2026-02-19T15:39:25"
source_files:
  - profile_gff3.config
  - profile_gff3.xml
  - profile_gff3.i1.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: profile
  parameters:
  input:
    # This parameter can be a single file (or a comma-separated list of such files) containing a new-line separated list of .bsml documents to be loaded
    INPUT_FILE_LIST: ""
    INPUT_FILE: ""
    INPUT_DIRECTORY: ""
    # the following is only used when iterating over an INPUT_DIRECTORY
    INPUT_EXTENSION: gff3
  output:
    OUTPUT_TOKEN: default
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PROF_OUTPUT_LIST: $;OUTPUT_DIRECTORY$;/$;COMPONENT_NAME$;.profile.list
  component:
    COMPONENT_NAME: profile_gff3
    DESCRIPTION: none
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    # The version,revision,tag here is set by an interpolated CVS tag
    VERSION: 2.0
    RELEASE_TAG: "$Name:  $"
    REVISION: "$Revision: 1.1 $"
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    ITERATOR1: i1
    ITERATOR1_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.$;ITERATOR1$;.xml
    # Distributed options
    GROUP_COUNT: 1
    NODISTRIB: 1
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - Preprocessing
    - Iterator
  command_sets:
    - profile_gff3 workflow
  includes:
    - "file=\"$;DOCS_DIR$;/file_iterator_template.xml\" keys=\"$;ITERATOR_NAME$;=ITERATOR1,$;ITERATOR_XML$;=ITERATOR1_XML\"/"
  steps:
    - name: create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: create temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;TMP_DIR$;
    - name: Create GFF3 Profile file list
      type: RunJavaUnixCommand
      executable: find
      arg: "$;OUTPUT_DIRECTORY$; -name '*.prof'"
      params:
        - key: stdout
          value: $;PROF_OUTPUT_LIST$;
        - key: stderr
          value: $;TMP_DIR$;/create_profile_file_list.stderr

# Iterator i1 - per-input processing steps
iterator_i1:
  command_sets:
    - profile_gff3
  steps:
    - name: Profile GFF3 file
      type: RunUnixCommand
      executable: $;BIN_DIR$;/gff3Profiler
      arg: $;I_FILE_PATH$; $;OUTPUT_DIRECTORY$;/gff3Profiler.pl.$;I_FILE_BASE$;.prof
      params:
        - key: stdout
          value: $;TMP_DIR$;/gff3Profiler.pl.$;I_FILE_BASE$;.stdout
        - key: stderr
          value: $;TMP_DIR$;/gff3Profiler.pl.$;I_FILE_BASE$;.stderr
