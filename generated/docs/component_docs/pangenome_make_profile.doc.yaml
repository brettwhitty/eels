---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: pangenome_make_profile
extracted_at: "2026-02-19T15:39:25"
source_files:
  - pangenome_make_profile.config
  - pangenome_make_profile.xml
  - component_docs/pangenome_make_profile.tmpl

# Component documentation (from Ergatis web UI help template)
component_documentation: |
  <!-- component documentation -->
  
  <p>
      This component does pangenome analysis. More documentation to follow.
  </p>
  <h3>references</h3>
  <p>
      To be completed.
  </p>
  <h3>input</h3>
  <p>
      Input is a binary file of merged and parsed all-vs-all blast results for a set of genomes. Usually this would
      include results from both BLASTP and TBLASTN.
  
  </p>
  <h3>bsml output</h3>
  <div class='bsml_container'>
      no bsml output defined for this component
  </div>
  <h3>output</h3>
  <p>
      To be completed.
  </p>

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: unclassified
  parameters:
  input:
    # Blast stored file created using pangenome_parse_blast
    BLAST_STORED_FILE: ""
    # A file listing database names to include in the analysis. a 'database name'
    # just means the uniquename prefix (for apx1.assembly.1.0 the database name
    # would be apx1).Leave blank for all organisms to be included.
    DB_FILTER_LIST: ""
  output:
    OUTPUT_TOKEN: default
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PANGENOME_PROFILE: $;OUTPUT_DIRECTORY$;/pangenome.profile.txt
  component:
    COMPONENT_NAME: pangenome_make_profile
    DESCRIPTION: Creates a profile matrix showing the genes that have BLAST hits for each genome
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    # The version,revision,tag here is set by an interpolated CVS tag
    VERSION: current
    RELEASE_TAG: $Name$
    REVISION: "$Revision: 4680 $"
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    ITERATOR1: i1
    ITERATOR1_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.$;ITERATOR1$;.xml
    # Distributed options
    GROUP_COUNT: 150
    NODISTRIB: 0
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - Preprocessing
    - hack to make sure these following commands will run other than master node
  command_sets:
    - pangenome_make_profile workflow
    - pangenome_make_profile distributed work
  steps:
    - name: create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: create temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;TMP_DIR$;
    - name: replace template keys
      type: RunUnixCommand
      executable: $;BIN_DIR$;/replace_template_keys
      params:
        - key: --template_xml
          value: $;DOCS_DIR$;/pangenome_make_profile_distributed_work.xml
        - key: --component_conf
          value: $;COMPONENT_CONFIG$;
        - key: --output_xml
          value: $;WORKFLOW_REPOSITORY$;/pangenome_make_profile_distributed_work.xml.gz
        - key: --distribopts
          value: nodistrib=$;NODISTRIB$;
        - key: stdout
          value: $;TMP_DIR$;/replace_template_keys.stdout
        - key: stderr
          value: $;TMP_DIR$;/replace_template_keys.stderr

# Perl script documentation (POD)
perl_scripts:
  - script: pangenome_make_profile.pl
    pod: |
  =head1  NAME
  
  do_pangenome_analysis.pl - Performs the merging and analysis of BLAST results to create pangenome data.
  
  =head1 SYNOPSIS
  
  USAGE: pangenome_query_list.pl
          --input_list=/path/to/somefile.dat.list
          --output=/path/to/output.list
        [ --log=/path/to/some/log ]
  
  =head1 OPTIONS
  
  B<--input_list,-i>
      List of files containing serialized array of BLAST data.
  
  B<--output_path,-o>
      Path to which output files will be written.
  
  B<--output_file,-o>
      Full path and name of pangenome data output file.
  
  B<--log,-d>
      optional. Will create a log file with summaries of all actions performed.
  
  B<--debug>
      optional. Will display verbose status messages to STDERR if logging is disabled.
  
  B<--help,-h>
      This help message/documentation.
  
  =head1   DESCRIPTION
  
      The pangenome analysis script creates an array of BLAST results data which is then
      processed to create pangenome data.
  
  =head1 INPUT
  
      The input should be a list of files containing serialized BLAST results array data.
  
  =head1 OUTPUT
  
      There is no output unless you use the --log option.
  
  =head1 CONTACT
  
      Brett Whitty
      bwhitty@tigr.org
