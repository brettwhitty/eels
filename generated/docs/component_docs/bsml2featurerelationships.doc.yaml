---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: bsml2featurerelationships
extracted_at: "2026-02-19T15:34:47"
source_files:
  - bsml2featurerelationships.config
  - bsml2featurerelationships.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: unclassified
  parameters:
    ADD_ASSEMBLY: 1
    # Add the genomic coordinates of the spcified feature type to the output
    ADD_FEATURE_COORDINATES: transcript
    # Comma separated list of feature classes.  Will determine the order of the columns
    # in the output file.
    OUTPUT_ORDER: "polypeptide,CDS,transcript"
  input:
    INPUT_FILE_LIST: ""
    INPUT_FILE: ""
    INPUT_DIRECTORY: ""
  output:
    OUTPUT_TOKEN: default
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
  component:
    COMPONENT_NAME: bsml2featurerelationships
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    DESCRIPTION: none
    # The version,revision,tag here is set by an interpolated CVS tag
    VERSION: $Name$
    RELEASE_TAG: $Name$
    REVISION: $Revision$
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  command_sets:
    - bsml2featurerelationships workflow
  steps:
    - name: create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: bsml2feature_relationships
      type: RunUnixCommand
      executable: $;BIN_DIR$;/bsml2featurerelationships
      params:
        - key: --bsml_input
          value: "'$;INPUT_FILE$;'"
        - key: --bsml_list
          value: "'$;INPUT_FILE_LIST$;'"
        - key: --bsml_dir
          value: "'$;INPUT_DIRECTORY$;'"
        - key: --output
          value: $;OUTPUT_DIRECTORY$;/bsml2featurerelationships.mapping.list
        - key: --output_order
          value: $;OUTPUT_ORDER$;
        - key: --add_assembly
          value: $;ADD_ASSEMBLY$;
        - key: --add_feature_coordinates
          value: $;ADD_FEATURE_COORDINATES$;
