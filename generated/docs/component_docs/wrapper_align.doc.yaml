---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: wrapper_align
extracted_at: "2026-02-19T15:39:25"
source_files:
  - wrapper_align.config
  - wrapper_align.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: RNA-Seq
  parameters:
    # use --v for verbose summary
    OTHER_ARGS: --v
  input:
    # Pipeline id
    PIPELINE_ID: ""
    # path to Pipeline output repository
    OUTPUT_REPOSITORY: ""
  output:
    # use a unique sample name
    OUTPUT_TOKEN: default
    # output directory for component results
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
  component:
    COMPONENT_NAME: wrapper_align
    DESCRIPTION: Summary of alignment statistics.
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    # script version
    VERSION: wrapper_align version 1.0.0
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    ITERATOR1: i1
    ITERATOR1_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.$;ITERATOR1$;.xml
    # Distributed options
    GROUP_COUNT: 150
    NODISTRIB: 0
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - PreProcessing
    - Processing
  command_sets:
    - wrapper_align
  steps:
    - name: create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: create temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;TMP_DIR$;
    - name: wrapper_align
      type: RunUnixCommand
      executable: $;BIN_DIR$;/wrapper_align
      arg: $;OTHER_ARGS$;
      params:
        - key: --pipeline
          value: $;PIPELINE_ID$;
        - key: --outrep
          value: $;OUTPUT_REPOSITORY$;
        - key: --outdir
          value: $;OUTPUT_DIRECTORY$;
        - key: stdout
          value: $;OUTPUT_DIRECTORY$;/wrapper_align.stdout
        - key: stderr
          value: $;OUTPUT_DIRECTORY$;/wrapper_align.stderr

# Perl script documentation (POD)
perl_scripts:
  - script: wrapper_align.pl
    pod: |
  =head1 NAME
  
  wrapper.pl          -script to combines various statistics output and creates a PDF.
  
  =head1 SYNOPSIS
  
      wrapper.pl      --p pipeline_id --r out_rep 
                      [--o outdir] [--v] [--help]
  
      parameters in [] are optional
      do NOT type the carets when specifying options
  
  =head1 OPTIONS
  
      --r <out rep>         = Path to the output repository.
  
      --p <pipeline id>     = ID of the pipeline whose summary is required 
  
      --o <output dir>      = /path/to/output directory. Optional. [present working directory]
  
      --v                   = generate runtime messages. Optional
   
      --help                = Help document.
  
  =head1 DESCRIPTION
  
  The script generates summary of the alignment statistics.
  
  =head1 AUTHOR
  
   Priti Kumari
   Bioinformatics Software Engineer 
   Institute for Genome Sciences
   University of Maryland
   Baltimore, Maryland 21201
