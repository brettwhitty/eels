---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: j_ortholog_clusters
extracted_at: "2026-02-19T15:34:47"
source_files:
  - j_ortholog_clusters.config
  - j_ortholog_clusters.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: clustering
  parameters:
    PVALCUT: 1e-5
    PCOVERAGE_CUTOFF: 70
    PIDENTITY_CUTOFF: ""
    PSIMILARITY_CUTOFF: ""
    MAX_MULTI_ALIGNMENT: 100
    # Jaccard coefficient cutoff for pruning edges from generated clusters - should be a value between 0 and 1, inclusive
    JACCARD_CUTOFF: 0.0
    # set this to 1 if the Seq-data-import.identifiers in the input BSML are not guaranteed to match the 
    # ids of the corresponding linked features
    USE_FEATURE_IDS_IN_FASTA: 0
  input:
    QUERY_BSML_FILE_LIST: ""
    MATCH_BSML_FILE_LIST: ""
    # List of BSML output files generated by clustalw component for a jaccard run
    JACCARD_OUTPUT_LIST: ""
  output:
    OUTPUT_TOKEN: default
    OUTPUT_DIRECTORY: $;REPOSITORY_ROOT$;/output_repository/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    FASTA_OUTPUT_LIST: $;OUTPUT_DIRECTORY$;/$;COMPONENT_NAME$;.fsa.list
  component:
    COMPONENT_NAME: j_ortholog_clusters
    DESCRIPTION: none
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    # The version,revision,tag here is set by an interpolated CVS tag
    VERSION: 2.0
    RELEASE_TAG: $Name$
    REVISION: $Revision$
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    # Distributed options
    GROUP_COUNT: 150
    NODISTRIB: 0
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - Preprocessing
    - Processing
    - hack to make sure these following commands will run other than master node
  command_sets:
    - j_ortholog_clusters workflow
    - j_ortholog_clusters distributed work
  steps:
    - name: create output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$;
    - name: create temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;TMP_DIR$;
    - name: replace template keys
      type: RunUnixCommand
      executable: $;BIN_DIR$;/replace_template_keys
      params:
        - key: --template_xml
          value: $;DOCS_DIR$;/j_ortholog_clusters_distributed_work.xml
        - key: --component_conf
          value: $;COMPONENT_CONFIG$;
        - key: --output_xml
          value: $;WORKFLOW_REPOSITORY$;/j_ortholog_clusters_distributed_work.xml.gz
        - key: --distribopts
          value: nodistrib=$;NODISTRIB$;
        - key: stdout
          value: $;TMP_DIR$;/replace_template_keys.stdout
        - key: stderr
          value: $;TMP_DIR$;/replace_template_keys.stderr
