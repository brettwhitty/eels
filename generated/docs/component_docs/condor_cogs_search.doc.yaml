---
# Ergatis Component Documentation
# Extracted from source by extract_component_docs.pl
# Source: TIGR/JCVI/IGS Ergatis bioinformatics workflow system
#
# This file preserves ALL documentation, comments, parameter descriptions,
# and structural details from the original Ergatis source files.

component: condor_cogs_search
extracted_at: "2026-02-19T15:39:24"
source_files:
  - condor_cogs_search.config
  - condor_cogs_search.xml
  - condor_cogs_search.i1.xml

# Configuration file - all sections, parameters, defaults, and inline comments
config:
  interface:
    classification: database / manipulation
  parameters:
    # Tanja's user account has the appropriate permissions for updating records in the databases.
    USERNAME: tanjad
    PASSWORD: tanjad9
  input:
    # condor_cogs_search control file is much like the legacy2bsml control file.
    # Should contain a listing of organisms and corresponding asmbl_ids
    CONTROL_FILE: ""
  output:
    OUTPUT_TOKEN: default
    OUTPUT_DIRECTORY: $;TMP_DIR$;
  component:
    COMPONENT_NAME: condor_cogs_search
    DESCRIPTION: none
    WORKFLOW_REPOSITORY: $;REPOSITORY_ROOT$;/workflow/runtime/$;COMPONENT_NAME$;/$;PIPELINEID$;_$;OUTPUT_TOKEN$;
    PIPELINE_TOKEN: unnamed
    # The version,revision,tag here is set by an interpolated CVS tag
    VERSION: 2.0
    RELEASE_TAG: $Name$
    REVISION: "$Revision: 4794 $"
    TEMPLATE_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.xml
    ITERATOR1: i1
    ITERATOR1_XML: $;DOCS_DIR$;/$;COMPONENT_NAME$;.i1.xml
    # Distributed options
    GROUP_COUNT: 1
    # no-distrib = 0 ensures that all executes in series and never parallel
    NODISTRIB: 0
    # the following keys are replaced at runtime by the invocation script
    COMPONENT_CONFIG: ""
    COMPONENT_XML: ""
    PIPELINE_XML: ""
    PIPELINEID: ""
  include:
    PROJECT_CONFIG: ""

# Main workflow XML template - step definitions
workflow_xml:
  xml_comments:
    - Preprocessing
  command_sets:
    - condor_cogs_search workflow
  includes:
    - "file=\"$;DOCS_DIR$;/iterator_template.xml\" keys=\"$;ITERATOR_NAME$;=condor_cogs_search,$;ITERATOR_XML$;=ITERATOR1_XML,$;ITERATOR_LIST$;=$;WORKFLOW_REPOSITORY$;/condor_cogs_search.list,$;NODISTRIB$;=1\"/"
  steps:
    - name: create output directory and temp directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;OUTPUT_DIRECTORY$; $;TMP_DIR$;
    - name: Create condor_cogs_search master iterator list file
      type: RunUnixCommand
      executable: $;BIN_DIR$;/create_legacy2bsml_iterator_list
      params:
        - key: --control_file
          value: $;CONTROL_FILE$;
        - key: --output
          value: $;WORKFLOW_REPOSITORY$;/condor_cogs_search.list
        - key: stdout
          value: $;TMP_DIR$;/create_condor_cogs_search_list.stdout
        - key: stderr
          value: $;TMP_DIR$;/create_condor_cogs_search_list.stderr

# Iterator i1 - per-input processing steps
iterator_i1:
  command_sets:
    - condor_cogs_search
  steps:
    - name: Create database output directory
      type: RunUnixCommand
      executable: mkdir
      arg: -p -m 777 $;WORKFLOW_REPOSITORY$;/$;DATABASE$;_$;ASMBL_ID$;
    - name: condor_cog_search.dbi
      type: RunUnixCommand
      executable: $;BIN_DIR$;/condor_cog_search
      arg: -u $;USERNAME$; -p $;PASSWORD$; -D $;DATABASE$; -a $;ASMBL_ID$;
      params:
        - key: stdout
          value: $;WORKFLOW_REPOSITORY$;/$;DATABASE$;_$;ASMBL_ID$;/condor_cog_search.dbi.stdout
        - key: stderr
          value: $;WORKFLOW_REPOSITORY$;/$;DATABASE$;_$;ASMBL_ID$;/condor_cog_search.dbi.stderr

# Perl script documentation (POD)
perl_scripts:
  - script: create_legacy2bsml_iterator_list.pl
    pod: |
  =head1  NAME 
  
  generate_asmbl_list.pl - Default output is a workflow iterator that
  can be used to iterator over a set of asmbl_ids
  
  =head1 SYNOPSIS
  
  USAGE:  generate_asmbl_list
  
  =head1 OPTIONS
  
  =item *
  
  B<--debug,-d> Debug level.  Use a large number to turn on verbose debugging. 
  
  =item *
  
  B<--log,-l> Log file
  
  =item *
  
  B<--help,-h> This help message
  
  =head1   DESCRIPTION
