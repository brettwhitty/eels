BLAST2BSML(1)                                                              User Contributed Perl Documentation                                                             BLAST2BSML(1)

NAME
       blast2bsml.pl  - convert raw blast output to BSML documents

SYNOPSIS
       USAGE:  blast2bsml.pl -i blastp.raw -o blastp.bsml -q /path/to/query_file.fsa -l /path/to/logfile.log

OPTIONS
       --input,-i
           The input raw blast output from the wu-blast suite.

       --output,-o
           The output BSML file name.

       --query_file_path,-q
           The full path to the query FASTA file (used to populate refxref)
           [When used in workflow should be passed ITER_FILE_PATH]

       --gzip_output,-g
           Compress the bsml output.  If there is not .gz extension on the the output file
           one will be added (optional).

       --max_hsp_count,-m Maximum number of HSPs stored per alignment (optional)

       --pvalue,-p Maximum P-value at and above which to exclude HSPs (optional)

       --filter_hsps_for_stats,-F Whether to filter the HSPs by best Sum(P) when calculating the %id/coverage/similarity for the seq-pair-alignment

       --class,-c The ref/comp sequence type.  Default is 'assembly'

       --split,-s Split the output into one file per query sequence even if the BLAST output represents multiple query sequences.  Default is off.

       --debug,-d
           Debug level.  Use a large number to turn on verbose debugging.

       --log,-l
           Log file

       --help,-h
           This help message

DESCRIPTION
       blastbtab2bsml.pl takes raw output from WU-BLAST and translates it to BSML.  In the case of an empty results set, a sequence stub and analysis link will still be created for the
       query sequence.

       NOTE:

       Calling the script name with NO flags/options or --help will display the syntax requirement.

CONTACT Brett Whitty bwhitty@tigr.org
perl v5.22.1                                                                           2017-11-14                                                                          BLAST2BSML(1)
